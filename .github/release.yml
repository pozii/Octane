# GitHub auto-generated release notes: flat "What's Changed" list in the
# style of "Subject (#PR) (@author)". Only noise is excluded; everything
# else lands in one list, and GitHub appends New Contributors automatically.
changelog:
  exclude:
    labels:
      - chore
      - skip-changelog
    authors:
      - dependabot
